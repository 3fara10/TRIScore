package org.example.networking.jsonprotocol;

public enum RequestType
{
    LOGIN,
    LOGOUT,
    REGISTER_REFEREE,
    GET_ALL_PARTICIPANTS_SORTED,
    GET_PARTICIPANTS_WITH_RESULTS_FOR_EVENT,
    ADD_RESULT,
    REGISTER_OBSERVER,
    UNREGISTER_OBSERVER
}
